# Server Configuration
PORT=3001

# Database (adjust based on your DB, e.g., Postgres, Supabase)
DATABASE_URL=

# Supabase Credentials (if using Supabase)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# n8n Webhooks (or other automation endpoints)
N8N_WEBHOOK_PLAYGROUND=
N8N_WEBHOOK_PRODUCTION=
N8N_WEBHOOK_PROMPT_ENGINEER=
N8N_WEBHOOK_RAG_SYNC=

# Evolution API (WhatsApp Integration)
EVOLUTION_API_URL=
EVOLUTION_API_KEY=

# OpenAI API Key
OPENAI_API_KEY=

# Google Cloud Credentials (Path within container or JSON content)
# Option 1: Path to the JSON file inside the container (if volume mounted)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/keyfile.json
# Option 2: Paste the JSON content directly (ensure proper handling)
GOOGLE_APPLICATION_CREDENTIALS_JSON=
